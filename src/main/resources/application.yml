spring:
  application:
    name: chatapp
  data:
    mongodb:
      username: user
      password: password
      host: localhost
      port: 27017
      database: chat_app
      authentication-database: admin

server:
  port: 8088
